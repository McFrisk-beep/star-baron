# Raw asset export (temporary)

Scratch branch — drop the Supabase Storage `sprites` bucket exports here, in
these matching folders. Filenames must stay exactly as Supabase has them
(don't rename anything); everything gets matched back to its slot by
filename and deleted from this branch once processed.

- `accessory/`, `blackbox/`, `blueprint/`, `commodity/`, `component/`,
  `contract/`, `extractor/`, `merc/`, `planet/`, `raceship/`, `shipart/`,
  `spacebg/`, `star/`, `station/` — drop the whole folder (or its zip) from
  Storage in here, matching name.
- `pagebg/` — same, its own folder/zip (largest one, ~62MB).
- `bgm/` — only `1785680161915_8kvfy.mp3` goes here (the one track still in
  the Music tab) — not the rest of that folder.

Zipped or unzipped is both fine either way.
